<div>
  <h1 class="font-bold text-2xl mb-3">Editando Tarefas</h1>

  <turbo-frame id="<%= dom_id @task %>">
    <%= render "form", task: @task %>
    <%= link_to "Lista de Tarefas", tasks_path, class: "btn mb-3 bg-gray-100" %>
  </turbo-frame>
</div>